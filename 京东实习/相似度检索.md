# 相似度检索

## 业务背景
在实体识别成功之后，我们需要对识别出的实体做embedding然后将这个embedding与向量库中的向量进行相似度检索，返回相似的实体
然后根据这些实体，再去进行结果的推荐

## 方案设计
1. embedding模型
这边说是使用预训练的bert模型，结合微调，然后对识别出的实体进行embedding。
注意的点:
为什么不使用Qwen3-Embedding-0.6B进行向量化？
解释：相比起bert Qwen3-Embedding-0.6B的准确度更高，但是速度慢很多。qps不达标，但是需要进一步测试吞吐量。按理说应该用vllm进行吞吐量的测试，需要自行测试。
2. 向量库架构设计
待定
3. reranker架构设计
待定