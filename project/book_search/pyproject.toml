[project]
name = "book-search"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "faiss-cpu>=1.13.2",
    "flask>=3.0,<4.0",
    "modelscope>=1.17.0",
    "numpy>=1.25",
]

[tool.pytest.ini_options]
markers = [
    "live: tests that call a real LLM endpoint with full key/base_url",
]

